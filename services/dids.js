async function processBatch(batchFileHash, merkleRoot) {
  // TODO download the batchFileHash
  // TODO process each of the items in the downloaded operationsArray and
  // update the local db with the latest state of each DID that is processed
  return;
}

module.exports = {processBatch};